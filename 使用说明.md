# 英语教练使用说明

## 每天怎么上课（三步）

1. 打开「终端」App（Terminal）。
2. 输入 `cd ~/english-agent` 然后回车（文件夹位置以实际存放为准）。
3. 输入 `codex` 然后回车，等它启动后，打「开始」再回车。

想结束时打「下课」，它会自动记好你今天学的所有东西，下次接着学。

## 你可以随时说的话

| 你说 | 它会做什么 |
|---|---|
| 开始 | 上今天的课：复习 + 新句子 + 情景对话 |
| 下课 | 记录今天的学习，总结 3 个重点 |
| 进度 | 告诉你学到哪了、哪里进步、哪里还弱 |
| 跟读 | 一句一句练发音 |
| XX怎么说 | 例如打「卸甲怎么说」，马上教你 |
| 只聊天 | 随便聊，基本不纠错 |
| 讲解 | 想知道「为什么这么说」的时候用 |

不记得口令也没关系，直接用中文说你想干嘛就行，它都懂。

## 听发音、练口语（重要）

- **电脑会读英语给你听。** 第一次读之前，屏幕会问「是否允许运行命令」。
  这里请选**带「don't ask again」字样的那一项**（大意是「以后 say 开头的
  命令都不用再问」），选完这一整节课就不会再打断你了。
  如果只选了普通的「允许（Yes）」，后面每读一句都会再问一次。
- **没声音就直接说「没声音」。** 教练会立刻改用文字标重音的方式继续上课，
  不会耽误你学。
- **你跟读时用语音输入：** 连按两下键盘上的 Fn（地球/麦克风）键，对着
  电脑读英文，读完再按一下结束。教练会听出你读得准不准。
- 第一次用语音输入需要开启：苹果菜单 → 系统设置 → 键盘 → 听写 → 打开，
  并在「语言」里添加英语（美国）。跟读前把听写语言切到英文。

## 给部署者：安装验证与看进度

1. 部署：整个文件夹放到学习者 Mac 上的 `~/english-agent`（如果放到别的位置，记得同步修改上面「每天怎么上课」第 2 步的 cd 路径）。
2. 结构检查：目录里应有 `AGENTS.md`、`curriculum/`（5 个文件）、
   `memory/`（4 个文件）、`sessions/`。
3. 发音检查（两步，缺一不可）：
   - 终端里直接跑 `say -r 175 "Welcome in!"`，应当出声。
   - **再在 Codex 里验证一遍**：这一步才是真的。Codex 默认把命令关在沙盒里
     跑，沙盒内 `say` 会「退出码 0、无报错、但完全没有声音」——终端里能出声
     不代表 Codex 里能出声，这正是之前踩过的坑。教练已被要求用沙盒外权限
     （escalated permissions）跑 `say`，第一次会弹批准框。
4. 冒烟：用 Codex 说「开始」跑一遍首课（应是中文破冰 + 教第一关 +
   say 出声），说「下课」后确认 `sessions/` 生成当日日志、`memory/`
   四个文件被更新。**注意听是否真的出声**，别只看它说「已朗读」。
5. 日常看进度：`memory/progress.md` 和 `sessions/` 最新日志；也可以
   `git pull` 后远程看（若已配置私有仓库）。
