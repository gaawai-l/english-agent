# 英语教练使用说明

## 每天怎么上课

**电脑上**（半分钟）：打开「终端」，输入 `cd ~/english-agent` 回车，
再输入 `claude` 回车，然后打「备课」回车。它会说「卡片已经复制好了」。

**手机上**（20-30 分钟）：

1. 打开 ChatGPT，**新建对话**，长按输入框**粘贴**，发送。
   （电脑刚复制的东西，手机上直接就能粘——两台设备共用一个剪贴板。）
2. 它回「准备好了」之后，点语音按钮，开始用说的上课。
3. 想结束就说「下课」，听它跟你道别。
4. 退出语音模式，往上翻到战报那条消息，点消息下面的「复制」。

**回电脑上**：打「备课」回车。它会自动读到你刚复制的战报，记录下来，
再把下一张卡片复制好——拿起手机粘贴就能接着练。

如果电脑没读到战报（或者两台设备剪贴板不通），就**把战报直接粘在
对话框里**，再打「备课」——一样管用，而且最保险。

小贴士：

- **觉得课太简单？** 语音里直接说「摸底」，它会越聊越难、摸清你的
  真实水平，之后的课就会跟上你。
- 一张卡片可以连着练好几天，不用每天都跑电脑。想换新内容时再跑。
- 忘了复制战报也没关系，这天就当白练，不影响之后上课。
- 手机上粘不出东西？说明两台设备离太远或蓝牙关了。让它靠近电脑、
  开着蓝牙和 WiFi 再试。**还是不行也不耽误上课**：备课时电脑屏幕上
  会把整张卡片打出来，用鼠标选中那一整段复制，发到微信「文件传输
  助手」或备忘录，再在手机上复制粘贴进 ChatGPT，一样能练。

## 备课完可以顺便问：

| 你说 | 它会做什么 |
|---|---|
| 进度（或「复盘」） | 学到哪了、哪里进步、哪里还弱 |
| XX怎么说 | 例如打「卸甲怎么说」，马上教你 |
| 讲解 | 想知道「为什么这么说」的时候用 |

## 手机不方便时：电脑也能上课（备用）

在电脑上直接打「开始」就是一节完整课；「下课」结束并记录；「跟读」
一句一句练发音；「只聊天」随便聊基本不纠错。

- 电脑会读英语给你听。用 Codex 第一次读会弹「是否允许」，请选
  **带「don't ask again」字样的那一项**；没声音就直接说「没声音」，
  教练会改用文字标重音继续上课，不耽误学。
- 跟读时用语音输入：连按两下 Fn（地球）键，对着电脑读英文，读完再
  按一下结束。（首次需开启：系统设置 → 键盘 → 听写 → 打开，并把
  「语言」加上英语（美国）；跟读前切到英文。）

## 给部署者：安装与验证

一次性安装（在她的设备上，约 5 分钟）：

1. **拉代码**：她 Mac 上 `git clone https://github.com/gaawai-l/english-agent.git ~/english-agent`
   （已经有了就 `cd ~/english-agent && git pull`）。
2. **确认通用剪贴板可用**（这是整套联动的通道）：两台设备登同一个
   Apple ID，都开蓝牙和 WiFi，且都开着「隔空播送与接力」里的**接力**
   （系统设置 → 通用 → 隔空播送与接力 → 接力）。
   验证：Mac 上复制一段字，iPhone 备忘录里粘贴，粘得出来就通了。
3. **ChatGPT 设置**（她 iPhone）：设置 → 个性化 → **清空自定义指令**
   （自定义指令对每次对话都生效，会和卡片打架）。「记忆」不用关——
   卡片开头已经要求它忽略既有印象，而关记忆会牵连她日常用 ChatGPT。
   万一发现它跳级或提没教过的内容，再改用「临时对话」练。
4. **端到端**：她 Mac 上跑一次「备课」（第一次 inbox 和剪贴板都空，
   属正常路径），确认它说卡片已复制；手机粘贴、真练两分钟、说「下课」、
   复制战报；回 Mac 再打一次「备课」，确认 memory/ 四个文件被更新、
   sessions/ 生成当日日志、新卡片已复制。
   **测的时候故意把同一个错犯两次**，这样才能验证 habits.md 那条链路。

排错：

- 手机粘不出卡片：两台设备要离得近、都开蓝牙和 WiFi、都开着「接力」。
  不行就走兜底——备课时终端里会把整张卡片打出来，选中复制，用微信
  「文件传输助手」发到手机即可；卡片同时也存在 iCloud 云盘 →
  Shortcuts → english → today-card.md，用「文件」App 打开复制也行。
- 电脑读不到战报：确认复制战报之后没有再复制过别的东西（剪贴板会被
  覆盖）。覆盖了就在手机上重新复制一次战报，再打「备课」。
- 电脑备用上课没声音：终端直接跑 `say -r 175 "Welcome in!"` 应出声；
  在 Codex 里必须再验一遍（沙盒内 `say` 退出码 0 但无声，教练已被
  要求用沙盒外权限跑）。
- 她 Mac 上这个仓库永远**只 pull 不 push**——远程仓库是公开的，
  学习记录是私人数据，不能上传。
