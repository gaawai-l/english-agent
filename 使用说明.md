# 英语教练使用说明

## 每天晚上：手机练口语（主要方式）

1. 点主屏幕的 📥「取今日卡片」——今天的课自动进了剪贴板。
2. 打开 ChatGPT，**新建对话**，长按输入框粘贴，发送。
3. 它回「准备好了」之后，点语音按钮，开始用说的上课。
4. 想结束就说「下课」，听它跟你道别。
5. 退出语音模式，往上翻到战报那条消息，点消息下面的「复制」。
6. 回主屏幕点 📤「存战报」——完成，记录会自动传回电脑。

小贴士：

- **觉得课太简单？** 语音里直接说「摸底」，它会越聊越难、摸清你的
  真实水平，之后的课就会跟上你。
- 忘了第 5-6 步没关系，第二天补做也行；实在忘了，这天就当白练，
  不影响之后上课。
- 关掉 ChatGPT 时手机会弹「记得存战报」的提醒，看到顺手点一下就好。

## 每隔几天：电脑备课（5-10 分钟）

1. 打开「终端」App，输入 `cd ~/english-agent` 回车。
2. 输入 `claude` 回车（装的是 Codex 就输入 `codex`）。
3. 打「备课」回车。它会读手机传回的记录、更新学习档案、备好下一张
   卡片，最后用中文告诉你最近进步在哪、还有哪里要注意。

不用天天跑。跑一次出的卡片可以连着练好几天——同样几句话换着场景练熟，
正是 0 基础记住它们的方式。想换新内容时再跑一次就行。

备课完可以顺便问：

| 你说 | 它会做什么 |
|---|---|
| 进度（或「复盘」） | 学到哪了、哪里进步、哪里还弱 |
| XX怎么说 | 例如打「卸甲怎么说」，马上教你 |
| 讲解 | 想知道「为什么这么说」的时候用 |

## 手机不方便时：电脑也能上课（备用）

在电脑上直接打「开始」就是一节完整课；「下课」结束并记录；「跟读」
一句一句练发音；「只聊天」随便聊基本不纠错。

- 电脑会读英语给你听。用 Codex 第一次读会弹「是否允许」，请选
  **带「don't ask again」字样的那一项**；没声音就直接说「没声音」，
  教练会改用文字标重音继续上课，不耽误学。
- 跟读时用语音输入：连按两下 Fn（地球）键，对着电脑读英文，读完再
  按一下结束。（首次需开启：系统设置 → 键盘 → 听写 → 打开，并把
  「语言」加上英语（美国）；跟读前切到英文。）

## 给部署者：安装与验证

一次性安装（在她的设备上，约 20 分钟）：

1. **拉代码**：她 Mac 上 `cd ~/english-agent && git pull`。
2. **ChatGPT 设置**（她 iPhone）：设置 → 个性化 → **清空自定义指令**
   （自定义指令对每次对话都生效，会和卡片打架）。「记忆」不用关——
   卡片开头已经要求它忽略既有印象，而关记忆会牵连她日常用 ChatGPT。
   万一发现它跳级或提没教过的内容，再改用「临时对话」练。
3. **生成首张卡片**：她 Mac 上跑一次「备课」（inbox 为空属正常路径），
   确认生成
   `~/Library/Mobile Documents/iCloud~is~workflow~my~workflows/Documents/english/today-card.md`。
4. **同步冒烟**：她 iPhone「文件」App → iCloud 云盘 → Shortcuts →
   english，能看到 today-card.md 即通（可能要等一两分钟）。
5. **快捷指令 ①「取今日卡片」**（iPhone 快捷指令 App → +）：
   - 动作「获取文件」：服务 iCloud Drive，**关闭「显示文档选择器」**，
     文件路径填 `/english/today-card.md`
   - 动作「拷贝到剪贴板」
   - 重命名「取今日卡片」，挑个 📥 图标，「添加到主屏幕」
6. **快捷指令 ②「存战报」**：
   - 动作「获取剪贴板」
   - 动作「追加到文本文件」：服务 iCloud Drive，文件路径填
     `/english/inbox/日期.md`——「日期」两个字不要打字，点路径框上方
     变量条里的「当前日期」插入，再点插进去的变量把格式改成
     **自定 → yyyy-MM-dd**
   - 重命名「存战报」，📤 图标，「添加到主屏幕」
7. **防遗忘提醒**：快捷指令 App → 自动化 → + → 「App」→ 选
   ChatGPT → 勾「已关闭」→「立即运行」→ 动作「显示通知」，内容：
   「练完了？复制战报后点一下 存战报」。
8. **端到端**：完整跑一遍她的六步（真用 ChatGPT 练两分钟即可），回
   Mac 跑「备课」，确认 memory/ 四个文件被更新、sessions/ 生成当日
   日志、明日卡片已刷新。

排错：

- 快捷指令报「找不到文件」：确认动作里服务是 iCloud Drive、路径以
  `/english/` 开头；交换区必须位于 iCloud 云盘的 **Shortcuts 文件夹**
  之下——这是快捷指令免弹窗按路径读写的唯一位置。
- 手机看不到文件/文件夹：「文件」App 下拉刷新；确认两台设备同一
  Apple ID 且 iCloud 云盘已开启。
- 电脑备用上课没声音：终端直接跑 `say -r 175 "Welcome in!"` 应出声；
  在 Codex 里必须再验一遍（沙盒内 `say` 退出码 0 但无声，教练已被
  要求用沙盒外权限跑）。
- 她 Mac 上这个仓库永远**只 pull 不 push**——远程仓库是公开的，
  学习记录是私人数据，不能上传。
